{
  "name": "rich-or-poor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seo": "node scripts/generate-sitemap.js && node scripts/generate-city-pages.js",
    "seo:sitemap": "node scripts/generate-sitemap.js",
    "seo:cities": "node scripts/generate-city-pages.js"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
